<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".parametrosControl">

    <!-- Control Button
        Boton que permite capturar el evento descrito en el código Java
        para poder cambiar de actividad y mostrar los valores de temperatura
        identificador btnTemp-->
    <Button
        android:id="@+id/btnTemp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="150dp"
        android:onClick="mostrarTemperatura"
        android:text="Temperatura" />

    <!-- Control Button
        Boton que permite capturar el evento descrito en el código Java
        para poder cambiar de actividad y mostrar los valores de recorrido
        identificador btnRecorre-->
    <Button
        android:id="@+id/btnRecorr"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="mostrarRecorrido"
        android:text="Recorrido" />

    <!-- Control Button
        Boton que permite capturar el evento descrito en el código Java
        para poder finalizar la activity establecida en ese instante
        identificador btnSalir-->
    <Button
        android:id="@+id/btnSalir"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="Salir"
        android:text="SALIR" />
</LinearLayout>